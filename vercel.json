{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.js"
    }
  ],
  "env": {
    "SQUARE_ACCESS_TOKEN": "@square_access_token",
    "DEV_TOKEN": "@dev_token"
  }
}
